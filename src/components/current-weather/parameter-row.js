export const ParameterRow = ({ label, value }) => (
  <div className="parameter-row">
    <span className="parameter-label">{label}</span>
    <span className="parameter-value">{value}</span>
  </div>
);
